# openlibing-ai-web 需求级 Spec

每个需求一个目录，命名建议：

```text
PR<需求号>_<需求短名>/
├── design.md
├── task.md
└── archive.md
```

开发前完成 `design.md` 和 `task.md`，评审通过后让 AI 按 `task.md` 执行。完成后补充 `archive.md`，并把可复用规则提炼到 `../ai_memory.md`。
