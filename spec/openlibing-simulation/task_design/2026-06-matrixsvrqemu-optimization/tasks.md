
# 实现步骤清单

## 1. 需求1：matrixSvrQemu调度逻辑优化

### 1.1 分析现有代码

- [x] 定位到`NodeManageServiceImpl.java`第456行的硬编码位置
- [x] 理解`getQemuEnDynamicValue`方法的调用链
- [x] 确认`configJson`的获取方式（`nodeManageMapper.getDeployTagsById("qemu")`）

### 1.2 配置解析实现

- [ ] 解析configJson获取`List<SimulationDeployConfigEntity>`
- [ ] 过滤`deploymentValue="matrixSvrQemu"`的配置项
- [ ] 从`DeploymentCategoryEntity`中提取`minCpu`和`minMemory`
- [ ] 处理配置项不存在的情况（回退到默认值）

### 1.3 修改调度逻辑

- [ ] 将硬编码的`baseCpu=32`改为从配置获取
- [ ] 将硬编码的`baseMemory=128`改为从配置获取
- [ ] 保持kvm模式的减半逻辑（`if (kvm) { baseCpu/=2, baseMemory/=2 }`）

### 1.4 测试验证

- [ ] 配置不同的minCpu/minMemory值进行测试
- [ ] 验证kvm模式下的计算正确性
- [ ] 验证配置缺失时的默认值回退机制

---

## 2. 需求2：机器分配并发安全问题（分布式锁 + 任务排队方案）

### 2.1 分布式锁配置

- [ ] 确认`DistributedLockService`已存在并可用
- [ ] 配置锁key：`node_allocation_lock`
- [ ] 配置锁超时时间：10秒
- [ ] 配置最大等待时间：3分钟
- [ ] 配置重试间隔：100毫秒

### 2.2 实体类更新（如需要）

- [ ] 检查`ServerUsingEntity`是否已存在，如不存在则新增

### 2.3 Mapper层实现

- [ ] 检查`ServerUsingMapper`是否已有`selectServerIdsByTaskId`方法，如无则新增
- [ ] 编写对应的SQL语句

### 2.5 Service层实现

- [ ] 修改`createOffcloudPolicyEnv`方法
- [ ] 实现分布式锁获取逻辑（最多等待3分钟）
- [ ] 实现异步分配机器逻辑（`allocateNodeAsync`）
- [ ] 实现锁释放逻辑（成功/失败都释放）
- [ ] 添加日志记录

### 2.6 异常处理设计

- [ ] 实现获取锁超时处理（抛出RuntimeException）
- [ ] 实现任务执行异常处理（捕获并记录）
- [ ] 实现锁过期场景处理（双重检查机器状态）
- [ ] 实现任务状态更新逻辑

### 2.7 并发测试

- [ ] 编写单元测试模拟并发请求
- [ ] 验证任务排队执行
- [ ] 验证无重复分配现象
- [ ] 验证锁超时自动释放

---

## 3. 需求3：/manage/task/node接口完善

### 3.1 业务逻辑层

- [ ] 在`getOffCloudById`方法内添加机器查询逻辑
- [ ] 根据`environmentId`（即`t_server_using`表中的`taskId`）查询
- [ ] 从`t_server_using`表查询任务正在使用的机器ID
- [ ] 从`server_basic_info`表批量查询机器详情
- [ ] 将`ServerBasicInfoEntity`转换为`NodeInfoEntity`

### 3.2 数据访问层

- [ ] 检查`ServerUsingMapper`是否已有`selectNodeListByTaskId`方法，如无则新增
- [ ] 编写JOIN查询SQL，直接返回`NodeInfoEntity`

### 3.3 密码脱敏处理

- [ ] 实现密码脱敏逻辑（统一替换为`******`）

### 3.4 返回格式调整

- [ ] 确保返回的`data`字段为`List<NodeInfoEntity>`类型
- [ ] 验证所有字段正确映射
- [ ] 处理任务不存在的情况（返回空列表）

### 3.5 接口测试

- [ ] 编写单元测试覆盖正常场景
- [ ] 测试边界条件（无效environmentId、空结果）
- [ ] 验证返回JSON格式符合预期

---

## 4. 代码质量保障

### 4.1 代码审查

- [ ] 检查代码格式符合项目规范
- [ ] 验证异常处理完整性
- [ ] 确认日志记录充分

### 4.2 单元测试

- [ ] 为新增逻辑编写单元测试
- [ ] 确保测试覆盖率≥80%
- [ ] 验证所有测试通过

### 4.3 集成测试

- [ ] 执行完整的集成测试套件
- [ ] 验证各组件间的协作正确性
- [ ] 确认无回归问题

---

## 5. 发布部署

### 5.1 依赖检查

- [ ] 确认分布式锁服务可用
- [ ] 检查数据库连接配置正确
- [ ] 验证配置文件已更新

### 5.2 灰度发布

- [ ] 先在测试环境部署验证
- [ ] 确认无问题后再部署到生产环境
- [ ] 监控部署后的接口调用情况

### 5.3 回滚方案

- [ ] 准备回滚脚本
- [ ] 确认数据库变更可回滚
- [ ] 记录发布时间点便于追踪

---

## 实现进度

```
┌─────────────────────────────────────────────────────────────────┐
│ 需求1：matrixSvrQemu调度逻辑优化                                  │
│  [ ] 配置解析实现                                                │
│  [ ] 修改调度逻辑                                                │
│  [ ] 测试验证                                                    │
├─────────────────────────────────────────────────────────────────┤
│ 需求2：机器分配并发安全问题                                        │
│  [ ] 数据库层优化                                                │
│  [ ] 分布式锁集成                                                │
│  [ ] 乐观锁实现                                                  │
│  [ ] 回滚机制                                                    │
│  [ ] 并发测试                                                    │
├─────────────────────────────────────────────────────────────────┤
│ 需求3：/manage/task/node接口完善                                  │
│  [ ] 数据访问层                                                  │
│  [ ] 业务逻辑层                                                  │
│  [ ] 密码脱敏处理                                                │
│  [ ] 返回格式调整                                                │
│  [ ] 接口测试                                                    │
├─────────────────────────────────────────────────────────────────┤
│ 代码质量保障                                                      │
│  [ ] 代码审查                                                    │
│  [ ] 单元测试                                                    │
│  [ ] 集成测试                                                    │
├─────────────────────────────────────────────────────────────────┤
│ 发布部署                                                          │
│  [ ] 依赖检查                                                    │
│  [ ] 灰度发布                                                    │
│  [ ] 回滚方案                                                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 问题与风险

| 问题/风险 | 描述 | 状态 | 解决方案 |
| :--- | :--- | :--- | :--- |
| 配置格式不匹配 | configJson的结构可能与预期不符 | 待确认 | 在解析前添加结构校验 |
| 分布式锁不可用 | Redis服务故障导致锁获取失败 | 待监控 | 添加锁获取失败的降级处理 |
| 数据库事务问题 | 多台机器分配时部分成功部分失败 | 待解决 | 使用数据库事务保证原子性 |
| 性能影响 | 加锁逻辑可能影响接口响应时间 | 待评估 | 通过压测验证性能影响范围 |
