# 实现任务清单

## 任务分解

### 1. tabs 结构改造

- [x] menu.vue 主页改为 div 实现 tabs 结构（tabList + activeTab + v-if）
- [x] 菜单 tab 保留原有内容
- [x] 各新 tab 内容拆分为独立子组件并懒加载挂载

### 2. 已下线接口 tab

- [x] 表格 + 分页 + 请求 URL 模糊查询
- [x] 操作列删除按钮（调删除接口）
- [x] 操作列确认误报/撤销误报按 flag 区分显示，分别调对应接口
- [x] 操作前二次确认、成功后刷新列表

### 3. 接口列表 tab

- [x] 表格 + 分页 + 请求 URL 模糊查询 + 接口用途/类型下拉过滤
- [x] 表格列：代码仓、请求方式、请求URL、接口用途/类型、操作
- [x] 修改弹窗（类型单选必填 + 确定/取消），确定调保存接口
- [x] 手动同步按钮调同步接口

### 4. 访问白名单接口 tab

- [x] 表格 + 分页 + 请求 URL 模糊查询
- [x] 新增弹窗（请求方式/URL 输入 + 三项白名单是/否单选）
- [x] 修改模式请求方式/URL 不可编辑，仅提交白名单字段
- [x] 删除按钮执行删除操作

### 5. 误报接口 tab

- [x] 表格 + 分页 + 请求 URL 模糊查询
- [x] 操作列撤销误报按钮调撤销误报接口

### 6. 菜单 tab URL 配置优化

- [x] URL 地址改为远程模糊过滤下拉（el-select remote）
- [x] 支持输入自定义值（allow-create + default-first-option）
- [x] flag=1 时下拉项与已选中回显为红色
- [x] 修改弹窗打开时 URL 值正确回显

### 7. API 层

- [x] url.ts 新增接口 URL 常量
- [x] api.ts 新增 RequestFunc 请求函数

### 8. 代码质量与交付

- [x] IDE 诊断检查无错误
- [x] 遵循仓库现有编码风格（Options API / apiClient.post / scoped less）
- [x] 提交代码至 fork 仓并创建业务 PR（关联 issue）
- [x] spec 文件归档至 openlibing-docs 仓并创建 docs PR

## 实现优先级

1. **P0（必须完成）**：tabs 结构改造、4 个新 tab 功能、API 层、URL 配置优化
2. **P1（重要）**：操作二次确认、失败兜底、分页查询过滤
3. **P2（可选）**：联调阶段接口路径/字段名核对微调

## 验证方式

- 本地启动前端服务，逐 tab 验证：切换、懒加载、分页、过滤、弹窗交互、操作按钮调接口
- 修改弹窗 URL 回显与红色标识验证（flag=1 场景）
- 回归菜单 tab 原有增删改查功能

## 负责人

- 开发：AI辅助开发
- 审核：项目团队成员
- 测试：开发自测 + 团队验证
