# 实现任务清单

## 任务分解

### 1. API 层

- [x] url.ts 新增 `GET_MENU_URL_DATA` 常量（query-menu-url-by-role）
- [x] api.ts 新增 `getMenuUrlData` 请求函数（post）

### 2. 角色详情接口列表面板

- [x] 详情弹窗（查看模式）新增「接口列表」标题行 + 接口地址搜索输入框
- [x] 新增表格：模块名称、接口地址（超长省略 + tooltip），限高 400px
- [x] 空数据展示 Empty 组件
- [x] 新增分页组件（10/20/50/100，页码与条数切换）
- [x] 新增 menuUrlList / menuUrl / urlPagination 状态与查询方法

### 3. 交互逻辑

- [x] 搜索回车/失焦触发查询并重置页码
- [x] 每页条数切换重置页码
- [x] 打开详情时初始化加载
- [x] 新增/编辑模式不显示该面板（v-if 按 dialogType 控制）

### 4. 代码质量与交付

- [x] IDE 诊断检查无错误
- [x] 遵循仓库现有编码风格（Options API / apiClient.post / table-common 样式）
- [x] 提交代码至 fork 仓并创建业务 PR（关联 issue）
- [x] spec 文件归档至 openlibing-docs 仓并创建 docs PR

## 实现优先级

1. **P0（必须完成）**：API 层、接口列表面板、搜索分页交互
2. **P1（重要）**：空数据兜底、模式约束
3. **P2（可选）**：联调阶段字段名核对微调

## 验证方式

- 本地启动前端服务，打开角色详情验证：
  - 接口列表自动加载、分页切换、搜索过滤生效
  - 新增/编辑模式不显示面板
  - 空数据显示 Empty
- 回归角色管理原有功能（增删改查、菜单权限配置）

## 负责人

- 开发：AI辅助开发
- 审核：项目团队成员
- 测试：开发自测 + 团队验证
