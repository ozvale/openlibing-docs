# 2026-08-26 新增项目表单组织/项目下拉框搜索 - 任务清单

## 实现步骤

- [x] 组织下拉框 `el-select` 添加 `filterable` 属性
- [x] 项目级联下拉框 `el-cascader` 添加 `filterable` 属性
- [x] 提交 commit（`acc8c038`）并推送 `feat-project-search` 分支
- [x] 创建业务 PR 并关联业务 Issue

## 验证记录

| 场景                           | 结果                            |
| ------------------------------ | ------------------------------- |
| 新增项目：组织下拉框输入关键字 | 选项按关键字过滤                |
| 新增项目：项目下拉框输入关键字 | 搜索并定位匹配项目节点          |
| 不输入关键字                   | 保持原有浏览选择行为            |
| 编辑项目（type='edit'）        | 不展示组织/项目下拉框，行为不变 |
