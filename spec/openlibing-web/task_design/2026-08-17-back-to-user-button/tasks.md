# 2026-08-17 管理中心返回主页按钮 - 任务清单

## 实现步骤

- [x] 在"收起菜单"按钮上方新增 `collapse-btn` 容器（复用既有样式类）
- [x] 按钮内放置 `HomeFilled` 图标 + `v-if="!isCollapse"` 控制的"返回主页"文字
- [x] 从 `@element-plus/icons-vue` 导入 `HomeFilled` 图标
- [x] 新增 `goBackToUser` 函数，点击执行 `router.push('/')`
- [x] 提交 commit（`dc051199`）并推送 `feat-back-to-user-button` 分支
- [x] 创建业务 PR 并关联业务 Issue

## 验证记录

| 场景             | 结果                                  |
| ---------------- | ------------------------------------- |
| 菜单展开态       | 显示 HomeFilled 图标 + "返回主页"文字 |
| 菜单折叠态       | 仅显示图标                            |
| 点击"返回主页"   | 跳转回根路由 `/`（主页）              |
| 点击"收起菜单"   | 折叠/展开行为不受影响                 |
| 管理中心菜单导航 | 正常                                  |
