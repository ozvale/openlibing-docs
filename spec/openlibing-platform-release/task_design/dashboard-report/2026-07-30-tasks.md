# 运营看板数据上报功能 — 任务分解

## Phase 1: 需求 (已完成)

- [x] brainstorming — 需求澄清
- [x] proposal.md — 需求背景 + 验收标准
- [x] design.md — 技术方案 + 影响范围

## Phase 2: 开发

- [x] task-1: 新增 `DashboardReportRequest` — 上报请求体 DTO（防御性拷贝 + unmodifiable 视图）
  - community, feature, businessMetrics, userMetrics, timestamp, repo 字段
  - 全参构造器 + Builder，Map 字段防御性拷贝
- [x] task-2: 新增 `DashboardBackfillRequest` — 补录请求体 DTO
  - userId, startDate, endDate 字段
- [x] task-3: 新增 `DashboardBackfillTaskStatus` — 补录任务状态 DTO
  - State 枚举: PENDING / RUNNING / SUCCESS / PARTIAL / FAILED
  - taskId, processedDays, successCount, failCount 等进度字段
- [x] task-4: 新增 `DashboardReportSchedule` — 定时上报核心类
  - `@Scheduled` 每日凌晨 1 点执行
  - Redis 分布式锁防重复
  - 全量社区获取（openeuler + 通用流程 projectIds）
  - 欧拉社区指标采集（MongoDB 5 个指标）
  - 通用流程指标采集（MySQL 5 个指标）
  - 平均时长计算（欧拉 + 通用）
  - Feign 上报调用
- [x] task-5: 新增 `DashboardBackfillService` — 历史补录服务
  - createTask: 生成 taskId，初始化任务状态
  - runBackfill: `@Async` 异步遍历日期范围，逐日调用 reportForDate
  - getTask: 查询任务进度
  - synchronized 防重复执行
- [x] task-6: 新增 `DashboardReportController` — 补录触发接口
  - `POST /dashboard/backfill` 日期校验 + 异步触发
- [x] task-7: 修改 `OpenlibingFrameworkClient` — 新增 Feign 方法
  - `reportDashboard(DashboardReportRequest)` 映射 `/manage/feature-dashboard/report`

## Phase 3: 质量门禁

- [ ] build — `mvn compile -q`
- [ ] test — 单元测试覆盖
- [ ] codecheck — 华为规范检查（G 系列规则）
- [ ] static — 无 TODO/FIXME，无硬编码凭证

## Phase 4: 交付

- [ ] PR — conventional commit + PR 描述
- [ ] Review — 6 维审查
- [ ] 归档 — archive/
