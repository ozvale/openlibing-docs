# 发布评审撤回与历史操作记录增强 - 实现任务

## 任务清单

### 1. DDL 改造

- [x] 1.1 在 `release_review_history.xml` 新增 `20260805_add_project_id_to_release_review_history` changeSet，用 `MARK_RAN` + `columnExists` 兼容已存在表
- [x] 1.2 验证原 `20260727_create_release_review_history` changeSet 内容未被修改，checksum 一致

### 2. 实体改造

- [x] 2.1 `ReleaseReviewHistoryEntity` 新增 `projectId` 字段（String 类型）

### 3. 撤回接口实现

- [x] 3.1 `ReleaseBaseService` 新增 `withdrawReleaseReview` 方法声明
- [x] 3.2 `ReleaseBaseServiceImpl.withdrawReleaseReview` 实现：
  - 校验评审单存在性、创建人权限
  - 更新评审单状态为 `SAVE`
  - 删除 `release_result` 表中该 `review_id` 对应数据
  - 插入「撤回评审单」操作历史，填充 `projectId`
- [x] 3.3 `ReleaseBaseController` 新增 `POST /base/withdrawReleaseReview` 接口

### 4. 操作历史分页查询接口

- [x] 4.1 新建 `ReviewHistoryPageQuery`（继承 `BasePageReqVO`，含 `reviewId` 必填校验）
- [x] 4.2 新建 `ReleaseReviewHistoryVo`（implements Serializable，含 7 个字段，`operationTime` 用 `@JsonFormat`）
- [x] 4.3 `ReleaseBaseService` 新增 `queryReviewHistoryPage` 方法声明
- [x] 4.4 `ReleaseBaseServiceImpl.queryReviewHistoryPage` 实现：
  - `selectPage` + `LambdaQueryWrapper` 按 `reviewId` 查询
  - `operationTime` 降序
  - `ConvertUtils.sourceToTarget` 转 VO
  - `PageData` 包装返回
- [x] 4.5 `ReleaseBaseController` 新增 `POST /base/queryReviewHistoryPage` 接口

### 5. 历史表插入点填充 project_id

- [x] 5.1 `ReleaseBaseServiceImpl.createReleaseReview` 插入历史时填充 `projectId`
- [x] 5.2 `ReleaseDecisionServiceImpl.startReleaseDecision` 插入历史时填充 `projectId`
- [x] 5.3 `ReleaseBaseServiceImpl.withdrawReleaseReview` 插入历史时填充 `projectId`

### 6. 验证

- [ ] 6.1 Liquibase 启动时 changeSet 正常执行（已存在列时 MARK_RAN 跳过）
- [ ] 6.2 创建评审单时 `release_review_history.project_id` 正确写入
- [ ] 6.3 发布决策时 `release_review_history.project_id` 正确写入
- [ ] 6.4 撤回评审单时状态回到 SAVE，`release_result` 对应数据被删除，历史表插入「撤回评审单」
- [ ] 6.5 撤回时不触碰 `release_field_items_review` 评审项表
- [ ] 6.6 非创建人撤回返回「无修改权限」
- [ ] 6.7 `POST /base/queryReviewHistoryPage` 分页返回正确数据，按 `operationTime` 降序
- [ ] 6.8 `pageNum`/`pageSize`/`reviewId` 参数校验生效

## 提交记录

| Commit     | 说明                                                                        |
| ---------- | --------------------------------------------------------------------------- |
| `70a336fd` | feat(review): add withdraw release review interface（撤回接口初版）         |
| `543f046d` | feat(history): add project_id to release_review_history and paged query API |
| `c902ed48` | feat(review): delete release_result rows on withdraw（撤回时删发布结果）    |

## 关联 Issue

本需求未单独创建业务 Issue，属于发布基础功能增强。
