# #1 【发布评审】版本发布评审中增加上传附件功能（测试报告等）]测试策略设计说明书

## 1. 基本信息

* **需求链接**: https://gitcode.com/openlibing/openlibing-web/issues/69
* **需求名称**:【发布评审】版本发布评审中增加上传附件功能（测试报告等）
* **核心目标**:
  增加上传附加功能，可以上传文档，图片等文件，并提供下载功能，在评审中提供给专家等查看
* **开发责任人**: zkhzkhz
* **测试责任人**: zkhzkhz

---

## 2. 测试维度确认

> **操作指南**：请依据需求分析阶段的标签勾选。勾选后，必须在“第 3 节”提供对应的测试用例或方案。

* [X] **功能自检测试**

> * **测试重点：** API 契约验证、业务逻辑分支覆盖、边界值测试。
>* **目的：** 确保功能实现符合设计预期。
>* **触发条件：** 强制执行,**可委托开发测试完成，测试完成验收**。



* [ ] **集成测试**

> * **测试重点：** 跨服务调用链路验证、上下游数据最终一致性、数据库/中间件版本兼容性。
>* **目的：** 消除组件间级联影响风险。
>* **触发条件：** 需求标签含 `need_itest`


---

## 3. 专项验证设计和执行详情

> 测试自检
>* [ ] **Task 闭环**: 架构设计说明书中定义的 **TASK** 是否均有对应的测试结果？
>* [ ] **证据留存**: 关键测试（如性能、安全扫描）是否附带了截图或报告链接？

### 3.1 功能测试专项

> 参考测试设计方向
> * API 语义验证：验证 HTTP 状态码（2xx, 4xx, 5xx）的使用是否符合 RESTful 规范。
> * 边界与非法输入：验证大数据量、空字段、特殊字符及非法 JSON 格式的拦截能力。
> * 业务状态机闭环：验证资源从“创建中”到“运行中”再到“已释放”的全生命周期逻辑。

**1.基础功能**:

- 前置条件：使用openLiBing发布功能
- 步骤：
  - 点击上传按钮
  - 选中10个不超过100MB的PDF文档 .pdf、Word文档 .doc,.docx、Excel表格.xls, .xlsx、压缩包 .zip, .rar, .7z 文件进行上传
* **对应task(issueID)链接:**
* **预期结果**: 上传成功

**2。边界测试_文件大小**:
- 前置条件：使用openLiBing发布功能
- 步骤：
    - 点击上传按钮
    - 选中超过100MB大小的压缩包（.zip, .rar, .7z） 文件进行上传
* **对应task(issueID)链接:**
* **预期结果**: 上传失败，出现文件大小过大提示语

**2。边界测试_文件数量**:
- 前置条件：使用openLiBing发布功能
- 步骤：
    - 点击上传按钮
    - 上传11个不超过100MB的PDF文档 .pdf、Word文档 .doc,.docx、Excel表格.xls, .xlsx、压缩包 .zip, .rar, .7z 文件进行上传
* **对应task(issueID)链接:**
* **预期结果**: 上传失败，出现上传文件数量过多提示

**3。边界测试_文件格式**:
- 前置条件：使用openLiBing发布功能
- 步骤：
    - 点击上传按钮
    - 分别上传.JPEG、.txt文件

* **对应task(issueID)链接:**
* **预期结果**: 上传失败，出现上传文件格式支持提示


### 3.3 集成测试专项

> 参考测试设计方向
> * 协议兼容性：验证不同版本的组件间（如旧版网关与新版后端）通过 gRPC/HTTP 通信的兼容性。
> * 分布式事务/最终一致性：验证跨数据库操作或跨服务调用时，在网络波动下的数据对齐情况。
> * 全链路追踪覆盖：确保 TraceID 在所有微服务节点间透传，无断链现象。

**1。下载附件**:
- 前置条件：使用openLiBing发布功能
- 步骤：点击附件下载按钮

* **对应task(issueID)链接:**
* **预期结果**: 下载到之前上传的附件

**2。删除附件**:
- 前置条件：使用openLiBing发布功能
- 步骤：删除之前上传的附件

* **对应task(issueID)链接:**
  * **预期结果**: 
    - 1、前端内容不显示已删除的附件
    - 2、obs桶中对应附件已删除



---


