# 安全编译选项构建产物包支持下载 测试策略设计说明书

## 1. 基本信息

* **需求链接**: https://portal.edevops.huawei.com/ipdproject/third/2105006905
* **需求编号**: US20260703140461
* **需求名称**: 安全编译选项构建产物包支持下载
* **核心目标**:
  验证安全编译选项（sec-option-scan）扫描后的构建产物包支持从 openlibing-cicd 平台下载，覆盖接口鉴权、文件下载链路、产物包在 OBS/对象存储中的可用性，以及文件大小、文件名校验等边界场景。
* **开发责任人**: 闫兆鸿
* **测试责任人**: 刘东方

---

## 2. 测试维度确认

> **操作指南**：请依据需求分析阶段的标签勾选。勾选后，必须在"第 3 节"提供对应的测试用例或方案。

* [x] **功能自检测试**

> * **测试重点：** 下载接口契约验证、文件流正确性、文件名/Content-Type、权限校验。
>* **目的：** 确保构建产物包能够被合法用户正确下载，且数据完整。
>* **触发条件：** 强制执行,**可委托开发测试完成，测试完成验收**。

* [ ] **体验测试**

> * **测试重点：** 站在用户角度进行体验使用，验证产品是否符合用户习惯。
>* **目的：** 满足用户需求，超出用户期望；判定产品是否能让用户快速的接受和使用。
>* **触发条件：** 需求标签含 `need_experience`

* [ ] **集成测试**

> * **测试重点：** 跨服务调用链路验证、上下游数据最终一致性、数据库/中间件版本兼容性。
>* **目的：** 消除组件间级联影响风险。
>* **触发条件：** 需求标签含 `need_itest`

* [ ] **安全与隐私测试**：

> * **测试重点：** 鉴权绕过测试、SQL/命令注入、敏感数据（PII）日志脱敏校验、SBOM 依赖漏洞扫描。
>* **目的：** 验证"纵深防御"机制是否生效，确保无隐私泄露。
>* **触发条件：** 需求标签含 `need_security`

* [ ] **可靠性与韧性测试**

> * **测试重点：** 故障注入（Chaos）。模拟网络丢包/延迟、进程意外溢出、磁盘 IO 满载后等异常情况下的系统自愈行为。
>* **目的：** 验证架构设计中的"面向失败设计"等能力。
>* **触发条件：** 涉及核心Core服务变更,且架构设计含可靠性与韧性设计。

* [ ] **可服务性与可观测性测试**

> * **测试重点：** 告警有效性验证、指标准确性抽检、排障手册实操演练、优雅停机验证。
>* **目的：** 确保系统"可感知、可定位、可维护"。
>* **触发条件：**  涉及核心Core服务变更,且架构设计含可服务性与可观测性设计。

* [ ] **性能与伸缩性测试**

> * **测试重点：** 基准测试、负载测试。验证延迟、吞吐量上限及资源配额（CPU/Mem）稳定性。
>* **目的：** 确保不产生性能退化，满足 SLO 要求。
>* **触发条件：** 涉及核心Core服务变更,且架构设计含性能与伸缩性设计。
---

## 3. 专项验证设计和执行详情

> 测试自检
>* [ ] **Task 闭环**: 架构设计说明书中定义的 **TASK** 是否均有对应的测试结果？
>* [ ] **证据留存**: 关键测试（如性能、安全扫描）是否附带了截图或报告链接？

### 3.1 功能测试专项

> 参考测试设计方向
> * 下载接口契约：验证 HTTP 状态码（200/401/403/404）使用规范、Content-Type、Content-Disposition。
> * 边界与异常：验证产物包不存在、产物包已被清理、查询参数缺失/异常情况。
> * 链路稳定性：验证下载过程对 OBS 鉴权失败、对象不存在等异常场景的兜底。

**1.下载接口可正常下载已上报的构建产物包**:

* **步骤一**：通过 sec-option-scan 插件上报一次扫描结果（包含 packageName），记录 packageName 与 recordId
* **预期结果**: 上报接口返回 2xx，记录可在概览接口中查询到
* **步骤二**：在 openlibing-cicd 前端安全编译选项页面的文件详情/概览页中点击产物包下载
* **预期结果**: 浏览器触发文件下载，下载文件与插件上报的 packageName 名称一致
* **步骤三**：解压下载的产物包，对比其内容与流水线构建产物
* **预期结果**: 文件内容完整无损坏，与流水线实际构建产物一致


**2.公共可下载链接**:

* **步骤一**：在 OBS/对象存储中将某次扫描对应的构建产物包设置为公共读权限
* **预期结果**: 产物包 ACL 配置为公共读
* **步骤二**：使用公网链接（无需登录）直接下载该产物包
* **预期结果**: 下载成功，文件内容完整，与 OBS 中产物包一致


**3.私有不可下载链接**:

* **步骤一**：在 OBS/对象存储中将某次扫描对应的构建产物包设置为私有权限
* **预期结果**: 产物包 ACL 配置为私有
* **步骤二**：使用公网链接（无凭证）尝试下载该产物包
* **预期结果**: 下载被拒绝，返回 403/未授权错误，无文件内容泄露
* **步骤三**：携带合法凭证后再次下载
* **预期结果**: 下载成功，文件内容完整


**4.安全编译选项扫描项可配置**:

* **步骤一**：在 sec-option-scan 插件或 openlibing-cicd 后台配置中，勾选/取消勾选部分安全编译选项（如 BIND_NOW、NX、PIC、PIE、RELRO、Run-time Search Path、SP、Strip 中的若干项）
* **预期结果**: 配置成功保存
* **步骤二**：使用新配置触发一次安全编译选项扫描
* **预期结果**: 仅对已勾选的选项进行扫描并产出结果，未勾选的选项不出现在概览与文件详情中
* **步骤三**：恢复默认配置后再次触发扫描
* **预期结果**: 全部选项恢复扫描，概览数据与文件详情中重新包含全部选项


**5.下载不存在的产物包_接口返回404**:

* **步骤一**：传入不存在的 recordId / packageName 调用下载接口
* **预期结果**: 接口返回 404 或约定的错误码，错误信息明确


**6.下载大体积产物包_下载过程稳定**:

* **步骤一**：选择 100MB 以上的大体积产物包触发下载
* **预期结果**: 下载过程稳定不中断，文件 SHA256 与原产物包一致


**7.文件名校验与Content-Disposition**:

* **步骤一**：下载包含中文、特殊字符文件名的产物包
* **预期结果**: Content-Disposition 头正确编码，浏览器下载文件名为原始文件名不出现乱码


---

