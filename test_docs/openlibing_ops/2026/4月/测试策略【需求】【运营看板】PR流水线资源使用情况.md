# #1 [需求名称] 测试策略设计说明书

## 1. 基本信息

* **需求链接**: https://gitcode.com/openlibing/openlibing-ops/issues/23
* **需求名称**: 【运营看板】PR流水线资源使用情况
* **核心目标**:
  验证功能正确性，以及架构设计中定义的安全与隐私、可靠性与韧性、可服务性与可观测性和性能与伸缩性等非功能专项任务的闭环验收。
* **开发责任人**: 樊元
* **测试责任人**: 马天宏

---

## 2. 测试维度确认

> **操作指南**：请依据需求分析阶段的标签勾选。勾选后，必须在“第 3 节”提供对应的测试用例或方案。

* [X] **功能自检测试**

> * **测试重点：** API 契约验证、业务逻辑分支覆盖、边界值测试。
>* **目的：** 确保功能实现符合设计预期。
>* **触发条件：** 强制执行,**可委托开发测试完成，测试完成验收**。

* [ ] **体验测试**

> * **测试重点：** 站在用户角度进行体验使用，验证产品是否符合用户习惯。
>* **目的：** 满足用户需求，超出用户期望；判定产品是否能让用户快速的接受和使用。
>* **触发条件：** 需求标签含 `need_experience`

* [ ] **集成测试**

> * **测试重点：** 跨服务调用链路验证、上下游数据最终一致性、数据库/中间件版本兼容性。
>* **目的：** 消除组件间级联影响风险。
>* **触发条件：** 需求标签含 `need_itest`

* [X] **安全与隐私测试**：

> * **测试重点：** 鉴权绕过测试、SQL/命令注入、敏感数据（PII）日志脱敏校验、SBOM 依赖漏洞扫描。
>* **目的：** 验证“纵深防御”机制是否生效，确保无隐私泄露。
>* **触发条件：** 需求标签含 `need_security`

* [X] **可靠性与韧性测试**

> * **测试重点：** 故障注入（Chaos）。模拟网络丢包/延迟、进程意外溢出、磁盘 IO 满载后等异常情况下的系统自愈行为。
>* **目的：** 验证架构设计中的“面向失败设计”等能力。
>* **触发条件：** 涉及核心Core服务变更,且架构设计含可靠性与韧性设计。

* [ ] **可服务性与可观测性测试**

> * **测试重点：** 告警有效性验证、指标准确性抽检、排障手册实操演练、优雅停机验证。
>* **目的：** 确保系统“可感知、可定位、可维护”。
>* **触发条件：**  涉及核心Core服务变更,且架构设计含可服务性与可观测性设计。

* [ ] **性能与伸缩性测试**

> * **测试重点：** 基准测试、负载测试。验证延迟、吞吐量上限及资源配额（CPU/Mem）稳定性。
>* **目的：** 确保不产生性能退化，满足 SLO 要求。
>* **触发条件：** 涉及核心Core服务变更,且架构设计含性能与伸缩性设计。
---

## 3. 专项验证设计和执行详情

> 测试自检
>* [ ] **Task 闭环**: 架构设计说明书中定义的 **TASK** 是否均有对应的测试结果？
>* [X] **证据留存**: 关键测试（如性能、安全扫描）是否附带了截图或报告链接？

### 3.1 功能测试专项
1. 代码仓名称列可点击跳转至对应代码仓页面
* **操作步骤**：1. 打开 PR 门禁看板汇总页面；2. 点击代码仓列中的蓝色代码仓名称（如 MindIE-LLM）
* **预期结果**:  成功跳转至该代码仓对应的详情页面，页面加载正常，显示该代码仓的 PR 门禁明细数据
2. PR 数量列数据统计准确
* **操作步骤**：1. 打开 PR 门禁看板汇总页面；2. 查看某代码仓 PR 数量列的总数；3. 点击该代码仓名称进入明细页，统计该仓下所有 PR 的数量
* **预期结果**:  汇总页面的 PR 总数与明细页实际 PR 数量一致，无遗漏或多算
3. 门禁 E2E 执行资源消耗数据（vCPU/NPU/Memory）显示正常
* **操作步骤**：1. 打开 PR 门禁看板汇总页面；2. 查看某代码仓门禁 E2E 执行对应的 vCPU（核时）、NPU（卡时）、Memory（GB*h）列数据；3. 进入该代码仓明细页，核对单条 PR 的资源消耗数据汇总结果
* **预期结果**:  汇总页面各资源消耗数据显示正常，无乱码、负数、格式错误，且与明细页数据汇总结果一致
4. 构建任务资源消耗数据（vCPU/NPU/Memory）显示正常
* **操作步骤**：1. 打开 PR 门禁看板汇总页面；2. 查看某代码仓构建任务对应的 vCPU（核时）、NPU（卡时）、Memory（GB*h）列数据；3. 进入该代码仓明细页，核对单条 PR 构建任务的资源消耗数据汇总结果
* **预期结果**:  汇总页面各资源消耗数据显示正常，无乱码、负数、格式错误，且与明细页数据汇总结果一致
5. 测试任务资源消耗数据（vCPU/NPU/Memory）显示正常
* **操作步骤**：1. 打开 PR 门禁看板汇总页面；2. 查看某代码仓测试任务对应的 vCPU（核时）、NPU（卡时）、Memory（GB*h）列数据；3. 进入该代码仓明细页，核对单条 PR 测试任务的资源消耗数据汇总结果
* **预期结果**:  汇总页面各资源消耗数据显示正常，无乱码、负数、格式错误，且与明细页数据汇总结果一致
6. PR 明细页单条 PR 门禁状态与汇总页数据对应
* **操作步骤**：1. 打开 PR 门禁看板汇总页面；2. 进入某代码仓明细页；3. 查看单条 PR 的门禁执行状态（成功 / 失败）、执行耗时、构建 / 测试任务状态
* **预期结果**:  明细页单条 PR 的门禁状态、耗时数据显示正常，与汇总页的统计口径一致，数据无冲突
7. 明细页 PR 的构建任务与测试任务状态显示正常
* **操作步骤**：1. 进入某代码仓 PR 明细页；2. 查看单条 PR 下构建任务、测试任务的成功 / 失败数量、最长耗时、排队耗时数据
* **预期结果**:  构建 / 测试任务的状态统计、耗时数据显示清晰，无格式错误，与实际流水线执行结果一致
8. 明细页 PR 的代码检查任务数据显示正常
* **操作步骤**：1. 进入某代码仓 PR 明细页；2. 滚动查看单条 PR 的代码检查任务模块；3. 核对任务成功 / 失败数量、最长耗时数据
* **预期结果**:  代码检查任务的状态、耗时数据显示正常，无乱码、缺失，与实际流水线执行结果一致